-- GARANTE QUE CARREGOU
repeat task.wait() until game.Players.LocalPlayer
repeat task.wait() until game.Players.LocalPlayer:FindFirstChild("PlayerGui")

local player = game.Players.LocalPlayer

-- POSIÇÕES
local brainrotPosition = Vector3.new(-812.01, 18, -346.84)
local basePosition      = Vector3.new(-819.31, 15, -385.16)
local divinePosition    = Vector3.new(-819.57, 1225, 2957.97)
local celestialPosition = Vector3.new(-819.57, 801, 2019.97)
local secretPosition    = Vector3.new(-819.57, 470.5, 1485.47)
local cosmicPosition    = Vector3.new(-819.57, 290.5, 1004.97)
local mythicalPosition  = Vector3.new(-819.57, 165.5, 651.97)
local legendaryPosition = Vector3.new(-819.57, 88.5, 180)
local epicPosition      = Vector3.new(-819.57, 101, 155.47)

-- GUI
local gui = Instance.new("ScreenGui")
gui.Parent = player:WaitForChild("PlayerGui")
gui.ResetOnSpawn = false

-- FRAME
local frame = Instance.new("Frame", gui)
frame.Size = UDim2.new(0, 280, 0, 680)
frame.Position = UDim2.new(0.5, -140, 0.5, -340)
frame.BackgroundColor3 = Color3.fromRGB(15,15,15)
frame.BorderSizePixel = 0
Instance.new("UICorner", frame).CornerRadius = UDim.new(0,15)

-- TOP BAR
local topBar = Instance.new("Frame", frame)
topBar.Size = UDim2.new(1,0,0,45)
topBar.BackgroundTransparency = 1

local title = Instance.new("TextLabel", topBar)
title.Size = UDim2.new(1,-50,1,0)
title.Position = UDim2.new(0,10,0,0)
title.BackgroundTransparency = 1
title.Text = "Sininho Hub"
title.TextScaled = true
title.Font = Enum.Font.GothamBold
title.TextColor3 = Color3.fromRGB(255,255,255)

-- MINIMIZAR
local minimizeBtn = Instance.new("TextButton", topBar)
minimizeBtn.Size = UDim2.new(0,35,0,35)
minimizeBtn.Position = UDim2.new(1,-40,0,5)
minimizeBtn.Text = "-"
minimizeBtn.TextScaled = true
minimizeBtn.BackgroundColor3 = Color3.fromRGB(40,40,40)
minimizeBtn.TextColor3 = Color3.fromRGB(255,255,255)
Instance.new("UICorner", minimizeBtn).CornerRadius = UDim.new(1,0)

-- SCROLL
local scroll = Instance.new("ScrollingFrame", frame)
scroll.Size = UDim2.new(1,-10,1,-50)
scroll.Position = UDim2.new(0,5,0,45)
scroll.CanvasSize = UDim2.new(0,0,0,800)
scroll.BackgroundTransparency = 1
scroll.ScrollBarThickness = 6

-- SOM
local clickSound = Instance.new("Sound", gui)
clickSound.SoundId = "rbxassetid://140072726814802"
clickSound.Volume = 1

-- NOTIF
local TweenService = game:GetService("TweenService")

local notifBg = Instance.new("Frame", gui)
notifBg.Size = UDim2.new(0, 320, 0, 50)
notifBg.Position = UDim2.new(0.5, -160, 1, -170)
notifBg.BackgroundColor3 = Color3.fromRGB(0,0,0)
notifBg.BackgroundTransparency = 0.2 -- 🔥 ajustado
notifBg.Visible = false
Instance.new("UICorner", notifBg).CornerRadius = UDim.new(0,12)

local notif = Instance.new("TextLabel", notifBg)
notif.Size = UDim2.new(1,0,1,0)
notif.BackgroundTransparency = 1
notif.TextColor3 = Color3.fromRGB(0,255,0)
notif.Text = "[✅] Success for Teleporting!"
notif.TextScaled = true

local function showNotification()
    notifBg.Visible = true

    -- RESET
    notifBg.BackgroundTransparency = 0.8
    notif.TextTransparency = 0

    task.wait(8)

    TweenService:Create(notifBg, TweenInfo.new(1), {
        BackgroundTransparency = 1
    }):Play()

    TweenService:Create(notif, TweenInfo.new(1), {
        TextTransparency = 1
    }):Play()

    task.wait(1)

    notifBg.Visible = false

    -- prepara próxima
    notifBg.BackgroundTransparency = 0.8
    notif.TextTransparency = 0
end

-- BOTÕES
local function createButton(text, y, color, pos)
    local btn = Instance.new("TextButton", scroll)
    btn.Size = UDim2.new(0, 230, 0, 55)
    btn.Position = UDim2.new(0.5, -115, 0, y)
    btn.Text = text
    btn.TextScaled = true
    btn.Font = Enum.Font.GothamBold
    btn.BackgroundColor3 = color
    btn.TextColor3 = Color3.fromRGB(0,0,0)
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0,12)

    btn.MouseButton1Click:Connect(function()
        clickSound:Stop()
        clickSound:Play()

        local char = player.Character or player.CharacterAdded:Wait()
        char:WaitForChild("HumanoidRootPart").CFrame = CFrame.new(pos + Vector3.new(0,5,0))

        showNotification()
    end)
end

-- BOTÕES
createButton("Pegue o Brainrot (segure na mão)",0,Color3.fromRGB(180,180,180),brainrotPosition)
createButton("Teleport to Base",70,Color3.fromRGB(120,120,120),basePosition)
createButton("Teleport to Divine Area",140,Color3.fromRGB(255,140,0),divinePosition)
createButton("Teleport to Celestial Area",210,Color3.fromRGB(255,204,0),celestialPosition)
createButton("Teleport to Secret Area",280,Color3.fromRGB(150,150,150),secretPosition)
createButton("Teleport to Cosmic Area",350,Color3.fromRGB(170,0,255),cosmicPosition)
createButton("Teleport to Mythical Area",420,Color3.fromRGB(255,100,200),mythicalPosition)
createButton("Teleport to Legendary Area",490,Color3.fromRGB(255,215,0),legendaryPosition)
createButton("Teleport to Epic Area",560,Color3.fromRGB(90,0,150),epicPosition)

-- MINIMIZAR
local minimized = false
minimizeBtn.MouseButton1Click:Connect(function()
    minimized = not minimized
    scroll.Visible = not minimized
    frame.Size = minimized and UDim2.new(0,280,0,45) or UDim2.new(0,280,0,680)
    minimizeBtn.Text = minimized and "+" or "-"
end)

-- ARRASTAR
local UIS = game:GetService("UserInputService")
local dragging, dragStart, startPos

topBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = frame.Position
    end
end)

UIS.InputChanged:Connect(function(input)
    if dragging then
        local delta = input.Position - dragStart
        frame.Position = UDim2.new(
            startPos.X.Scale,
            startPos.X.Offset + delta.X,
            startPos.Y.Scale,
            startPos.Y.Offset + delta.Y
        )
    end
end)

topBar.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = false
    end
end)
